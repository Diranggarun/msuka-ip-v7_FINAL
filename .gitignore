# Dependencies
node_modules/
**/node_modules/

# Environment & secrets
.env
.env.local
.env.*.local

# Uploads (user-generated content — private dir since the auth-route change; legacy path kept for old checkouts)
msuka-ip-v7/uploads/
msuka-ip-v7/public/uploads/

# Test artifacts
msuka-ip-v7/playwright-report/
msuka-ip-v7/test-results/

# Legacy / backups
MSUkaIP-legacy-backup.zip
_zip_inspect/

# Editor / OS
.vscode/
.idea/
.DS_Store
Thumbs.db

# Claude Code — local state stays ignored; project agents are shared config
.claude/*
!.claude/agents/
memory/

# Third-party agent skills installed via `npx skills add`. Downloaded content,
# reproducible from skills-lock.json — treated like node_modules, not committed.
.agents/

# Skill scratch output (impeccable critiques) and throwaway design mockups —
# working material for a redesign, not part of the shipped app.
.impeccable/
admin-redesign-mockup.html
ADMIN-REDESIGN-PROMPT.md

# Logs
*.log
npm-debug.log*

# TLS certs (self-signed, generated on first run)
msuka-ip-v7/certs/

# SQLite database (live data — never commit)
msuka-ip-v7/msukaip.db
msuka-ip-v7/msukaip.db-shm
msuka-ip-v7/msukaip.db-wal
msuka-ip-v7/backups/
