# MSUkaIP — finishing plan (deadline: Monday)

Today is **Thursday 6 August 2026**. Four working days.

The plan is ordered by *what blocks what*, not by what is easiest. One thing on
this list depends on other people, so it starts today and everything else fills
in around it.

---

## The one blocker: you have no evaluation data

The `survey_responses` table holds 124 rows and **every one was generated by the
automated test suite** (`smoke_1786022792631`, `PW Feedback 1786022722761`).

Chapter 4 *is* the analysis of that data. Without real respondents there is
nothing to compute, and inventing numbers is the one mistake that cannot be
recovered from in a defense.

**So collection starts today and runs in the background all week.**

---

## Thursday (today)

### 1. Clear the test rows — 2 minutes

Do this **before** anyone submits, or real responses land among 124 fakes with no
way to tell them apart.

```
cd msuka-ip-v7
node scripts/backup.js          # snapshot first
node scripts/clean-test-data.js
```

Confirm: `survey_responses` should read **0**.

### 2. Open collection — the rest of the week

- Start the server: `node server.js`
- Share `https://<your-lan-ip>:3443/feedback.html` on the CICS wifi
- Target **20–30**. If you can only reach 10–15, that is still a legitimate
  evaluation — it is declared as a limitation in Chapter 5, not hidden.

Ask respondents to fill the **Section E comment box**. Chapter 4 quotes those,
and a paper with real quotes reads very differently from one with only means.

### 3. Do NOT run the test suite again until collection closes

**Three specs submit survey responses**, not one — `api.spec.js`,
`feedback-form.spec.js` and `smoke.spec.js`. A full run adds roughly a dozen
synthetic rows, and they are indistinguishable from real ones in the dashboard
until you look at the names.

If you must run tests, run `node scripts/clean-test-data.js` immediately
afterwards — but that also deletes any genuine responses collected up to that
point, so collect a second batch after. Simplest: leave the tests alone until
Saturday, run them once, clean, and record the number.

### 4. Meanwhile — paper revisions to Chapters 1–3

`MSUkaIP_Paper_Revisions (1).docx` already lists seven specific edits. Work
through them in order; they are all mechanical find-and-replace:

1. §1.3 Specific Objective 3 — rewrite
2. §1.4 Scope — replace the paragraph
3. §2.1.3.4 — new section on RA 10173
4. §3.1.2 Functional Requirements — revise 2 rows, add 3, replace the Security row
5. §3.2.5 Architectural Design — replace items C, D, E (WebRTC / SQLite / in-memory state)
6. §3.3.1 Software Specification — replace tables A, B, C, F; add the HTTPS row to D
7. Keep the §7 justifications for the oral defense

---

## Friday

### 5. LAN smoke test — the last system deliverable

`DEPLOYMENT.md` §7. This is the item `PROGRESS.md` has flagged for weeks, and the
only one that needs hardware you do not have at your desk.

- Server on the college wifi, `https://<ip>:3443`
- A second device joins, registers, gets approved
- Send a text, an image, a file, a push-to-talk voice message
- Place a **1:1 voice call** between two devices and confirm audio both ways
- Screenshot everything — these become Chapter 4 figures

If ICE stalls on `checking`, both devices must be on the same `/24`. There is no
STUN by design.

### 6. Chapter 4 skeleton

Build every table and heading now, with the numbers left blank. When the data
closes on Saturday the chapter fills in rather than being written from scratch.

---

## Saturday

### 7. Close collection, export, analyse

- Admin → Feedback → **Export CSV**
- The export now includes the `notes` column
- The dashboard shows the overall weighted mean, per-section means, and how many
  respondents left a comment

### 8. Write Chapter 4 from the real numbers

Structure that follows from what the system actually produces:

- Respondent profile — count, by type, by device (all in the CSV)
- Section A–D weighted means, each with its Likert interpretation
- Overall weighted mean
- Qualitative findings — grouped themes from the comments, with direct quotes
- System testing results — Playwright pass counts, ESLint clean, the security
  tests (IDOR, XSS, SQL injection, rate limiting, traversal)

### 9. Re-run the full test suite and record the number

```
npx playwright test
```

Record the pass count and the date. Note the known Firefox feedback-form flake so
the number is reproducible rather than surprising. **Then clean the test data one
final time** if it touched the survey table.

---

## Sunday

### 10. Chapter 5 — Summary, Conclusions, Recommendations

Follows from Chapters 1–3 and the system, not from the survey means, so it can be
drafted before the data is perfect.

- Summary of findings
- Conclusions, mapped **one per specific objective** (see below)
- Recommendations — future work: STUN/TURN for off-LAN use, multi-server with a
  shared cache, video calls, mobile app
- Limitations — sample size, single-LAN deployment, encryption at rest is not
  end-to-end

### 11. Objective coverage check — the "100%" question

For each specific objective, write one line naming the evidence:

| Objective | Evidence to cite |
|---|---|
| Real-time messaging | Playwright group 3; Global Chat + private + group |
| File and image sharing | Playwright group 4; AES-256-GCM at rest; 5MB cap |
| Voice communication | WebRTC 1:1 call; LAN smoke test screenshots |
| CICS network integration | institutional email gate; admin approval; audit logs |
| Security / RA 10173 | bcrypt 12, AES-256-GCM, rate limiting, token revocation, IDOR + XSS fixes |
| Evaluation | Likert survey, N respondents, overall weighted mean |

If any row has no evidence, that is the gap to close first — not the prose.

### 12. Full read-through

Check the paper against the guidelines PDF for format: margins, font, spacing,
caption style, reference format, table numbering. **I could not read that PDF —
its font encoding defeated text extraction — so this check is yours.**

---

## Monday

### 13. Final pass and submit

- Chapter 4 numbers match the CSV exactly — re-check every figure
- Every table and figure is numbered and referenced in the text
- References complete, including RA 10173
- `DEFENSE.md` §9b for the security questions; §7 for the stack-change
  justifications

---

## What is already done

Nothing on the system side is outstanding except the smoke test.

- All features built and working
- **~220 Playwright tests passing**, ESLint clean
- Security: IDOR fixed and proven, stored XSS fixed, SQL injection blocked, rate
  limiting, session revocation, audit logging, encryption at rest
- Admin dashboard: overview with selectable metrics, filterable audit log,
  broadcast, group management, backup, login history
- Feedback form with the comment box, CSV export including comments
- Docs current: `PROGRESS.md`, `DECISIONS.md`, `DESIGN.md`, `DEBUGGING.md`,
  `DEFENSE.md`, `DEPLOYMENT.md`

---

## If you fall behind

Cut in this order:

1. **Do not cut** the smoke test or data collection — they are the deliverables
2. Cut Chapter 4's qualitative theming; report the means and quote 3–4 comments
3. Cut recommendation detail in Chapter 5 to a bulleted list
4. Cut nothing from the objective-coverage table — that is what "100% of
   objectives" is judged on
